## v0.1 — 초기 설계

### 핵심 목표
- 사용자의 시급을 기준으로 선택지 A/B의 **총비용(Total Cost Index)**을 계산·비교하고,
- 결과와 함께 **근거(시간비용/직접비용 분해 + 수식)**를 제공하여 의사결정의 확신을 높인다.

### 핵심 기능(MVP)
1) 시급 입력 → 분당 가치 자동 계산  
2) 선택지 A/B 입력: 소요시간(분), 직접비용(원)  
3) 결과 출력: A 총비용, B 총비용, 추천 선택, 차액(원)  
4) 근거 제공: 직접비용/시간비용 분해 + 계산식 토글  
5) Total Cost Index(=총비용)로 비교 결과를 직관적으로 표시  
6) 프리셋 예시 제공(온보딩)

### 계산 모델(정의)
- 총비용(TC):  
  $$TC = C + \left( \frac{W}{60} \times T \right)$$  
  - W: 시급(원/시간), T: 소요시간(분), C: 직접비용(원)
- 소수점 처리: **원 단위 절삭(floor)**

### 엣지 케이스(초기 포함 범위)
- 시급 0/음수/문자 입력 차단(오류 메시지)
- 시간 0분 허용(시간비용 0)
- 비용 0원 허용(무료 옵션)
- 총비용 동일 시 “차이 없음(동일)” 표시
- 비현실적 큰 값 입력 시 경고(계산은 유지)
- 단위 혼동(시/분) 예방(UI 예시 또는 변환 지원)

---

## 변경 기록

| 날짜 | 버전 | 변경 요약 | 변경 이유(1줄) | 영향(1줄) | 증빙(커밋/PR) |
|---|---|---|---|---|---|
| 2026-02-12 | v0.1 | 문제 정의 및 초기 설계 문서화 | 과제 요구사항에 맞는 문제 정의/기능 범위/엣지 케이스를 명시적으로 남기기 위해 | 이후 구현 단계에서 설계 기준으로 사용 | commit: initial-planning |
| 2026-02-12 | v0.5 | Spring Boot 기반 2안 비교 MVP 구현 (A/B 비교, 계산 로직, 기본 UI) | 실제 동작하는 웹앱 프로토타입을 빠르게 확보하여 기능 정확성을 검증하기 위해 | A/B 비교, 총비용/차액/추천 선택지 계산이 가능해짐 | commit: springboot-mvp |
| 2026-02-12 | v0.8 | 예외 처리/입력 검증/테스트 코드/문서화 보강 | 핵심 로직에 대한 신뢰도를 높이고, 평가 기준(기능 정확성/개선 과정/맥락 관리)을 충족하기 위해 | 잘못된 입력에 대한 방어 코드와 단위/통합 테스트, API 문서, README 실행 가이드가 추가됨 | commit: validation-and-tests |
| 2026-02-12 | v1.0 | 다안 비교·히스토리·캐싱·배포 준비까지 포함한 완성 버전 | 시간 제약 안에서 부가 기능과 배포 준비까지 포함해 “실제 서비스에 가까운 상태”로 마무리하기 위해 | 3~5개 선택지 비교, localStorage 히스토리, 인메모리 캐시, Docker/Heroku/Railway/AWS/GCP 배포 가이드가 추가됨 | commit: multi-and-deploy |
